<i class="bi bi-@(Icon) @(Class) @(string.IsNullOrEmpty(ColorScheme)?"":$"text-{ColorScheme}")"></i>

@code {
    [Parameter] public string Icon { get; set; } = "";
    [Parameter] public string Class { get; set; } = "";
    [Parameter] public string ColorScheme { get; set; } = "";
}